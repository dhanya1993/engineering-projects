{
  "name": "realtime-fleet-server",
  "version": "1.0.0",
  "description": "Node.js + Socket.IO backend simulating a live IoT device fleet — no database required, just run and watch the dashboard update.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
